import { assert } from 'chai';
import Tree from '../lib/Tree';

describe('index', function () {
  let tree;


  beforeEach(() => {
    tree = new Tree()
  })

  // it('should exist', () => {
  //   expect(tree.populate)
  // })

});

